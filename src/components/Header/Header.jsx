import styles from './Header.module.scss';

const Header = () => (
  <header className={styles.container}>
    <div>
      <h3>
        Welcome to my site
      </h3>
    </div>
  </header>
);

export default Header;
